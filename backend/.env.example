# Copie este arquivo para .env e ajuste os valores
PORT=3001
JWT_SECRET=troque_esta_chave_secreta

# Usado apenas pelo "npm run sync" (models Sequelize / PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/safecash

# Validade do token (ex.: 15m, 2h, 7d)
JWT_EXPIRES_IN=7d
